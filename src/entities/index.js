/**
 * Entidades do sistema
 * 
 * Este arquivo exporta todas as entidades disponíveis no sistema.
 * As entidades definem a estrutura e validações dos dados.
 */

const Product = require('./Product');

module.exports = {
  Product
};
